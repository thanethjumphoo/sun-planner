const sql = require('mssql'); const config = { user: 'sa', password: 'Your_password123', server: '127.0.0.1', database: 'sun_planner', options: { encrypt: false, trustServerCertificate: true } }; async function run() { try { await sql.connect(config); const result = await sql.query("SELECT TOP 20 so_number, erp_order_item_code, item_desc, planned_production_date FROM mps_plan_order ORDER BY id DESC"); console.dir(result.recordset); sql.close(); } catch(e) { console.error(e); } } run();
